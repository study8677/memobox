interface:
  display_name: "Index MemoBox"
  short_description: "Inspect project and global memory indexes"
  brand_color: "#2563EB"
  default_prompt: "Use $index-memory to inspect the MemoBox memory index when useful."
policy:
  allow_implicit_invocation: true
