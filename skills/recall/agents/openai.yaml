interface:
  display_name: "Recall MemoBox"
  short_description: "Review project and global memory inboxes"
  brand_color: "#2563EB"
  default_prompt: "Use $recall to review the MemoBox index inbox before starting this task."
policy:
  allow_implicit_invocation: true
