interface:
  display_name: "Write MemoBox"
  short_description: "Write model-readable memory"
  brand_color: "#2563EB"
  default_prompt: "Use $write-memory to write one MemoBox memory record when useful."
policy:
  allow_implicit_invocation: true
