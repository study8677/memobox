interface:
  display_name: "Using MemoBox"
  short_description: "Use MemoBox memory files"
  brand_color: "#2563EB"
  default_prompt: "Use $using-memobox to inspect MemoBox files, write records, or maintain memories."
policy:
  allow_implicit_invocation: true
