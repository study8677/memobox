interface:
  display_name: "Using MemoBox"
  short_description: "Use the MemoBox memory store"
  brand_color: "#2563EB"
  default_prompt: "Use $using-memobox to inspect, read, write, or maintain MemoBox memory records."
policy:
  allow_implicit_invocation: true
